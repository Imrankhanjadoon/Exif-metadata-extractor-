\# EXIF Metadata Extractor



\## 📌 About

This project extracts EXIF metadata (camera info, GPS data, date/time, etc.) from JPEG images and saves the results into a CSV report.



\## 📂 Project Structure

\- `exif\_project.py` → Main Python script

\- `sample\_images/` → Example images for testing

\- `outputs/` → CSV report generated after running the script

\- `requirements.txt` → Required Python libraries



\## 🚀 How to Run

1\. Install dependencies:

&nbsp;  ```bash

&nbsp;  pip install -r requirements.txt

